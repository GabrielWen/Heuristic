'use strict';

var React = require('react');
var Button = require('react-bootstrap').Button;

var Game = React.createClass({
  render: function() {
    return (
      <h1>JSX TEST</h1>
    );
  }
});

module.exports = Game;
